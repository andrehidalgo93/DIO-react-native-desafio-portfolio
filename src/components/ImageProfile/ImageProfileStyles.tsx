import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    resizeMode: 'contain',
    borderRadius: 100,
    borderWidth: 1,
    borderColor: '#53277E',
  },
});

export default styles;